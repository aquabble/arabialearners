import { useState } from 'react';import { Card, CardBody, CardTitle, CardSub } from './ui/Card.jsx';import Button from './ui/Button.jsx';export default function Drills({user}){const [count,setCount]=useState(0);return (<Card><CardBody><CardTitle>Drills</CardTitle><CardSub>Build speed & recall</CardSub><div className='small mb-16'>All drill interactions render here, now on a clean surface with proper spacing.</div><div className='flex items-center gap-16'><Button variant='brand' onClick={()=>setCount(c=>c+1)}>Start Drill</Button><div className='small'>Completed: {count}</div></div></CardBody></Card>) }