import { useState } from 'react';import { Card, CardBody, CardTitle, CardSub } from './ui/Card.jsx';import Button from './ui/Button.jsx';import Progress from './ui/Progress.jsx';export default function WordOrderGame({user}){const [progress,setProgress]=useState(24);return (<Card><CardBody><CardTitle>Word Order</CardTitle><CardSub>Build natural phrasing</CardSub><div className='small'>Your draggable tiles mount here. Layout provides breathing room and contrast.</div><div className='mt-16'><Progress value={progress}/></div><div className='mt-16'><Button variant='brand' onClick={()=>setProgress(p=>Math.min(100,p+12))}>Try one</Button></div></CardBody></Card>) }